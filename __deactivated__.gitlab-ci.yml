variables:
  GIT_SUBMODULE_STRATEGY: recursive
  LATEX_IMAGE: registry.gitlab.com/islandoftex/images/texlive:TL2026-2026-03-15-full

build-pdf:
  image: $LATEX_IMAGE
  script:
    - latexmk -pdf thesis.tex
  artifacts:
    paths:
      - "thesis.pdf"

